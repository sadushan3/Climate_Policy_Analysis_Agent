# Frontend

React + Vite UI for the Climate Policy Intelligence API. See the [root README](../README.md).

```bash
npm install
npm run dev      # proxies /api to http://127.0.0.1:8000
npm run lint
npm run build
```
